[build]
target = "aarch64-linux-android"

[target.aarch64-linux-android]
rustflags = ["-C", "link-arg=-Wl,--gc-sections", "-C", "strip=symbols"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
