[package]
name = "hmem_jni"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"

[lib]
name = "hmem_jni"
crate-type = ["cdylib"]

[dependencies]
jni = { version = "0.22", default-features = false }
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
